apiVersion: v1
kind: Service
metadata:
  name: hpc-dashboard-service
  namespace: hpc-dashboard
  labels:
    app: hpc-dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: hpc-dashboard


